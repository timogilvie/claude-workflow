Pre-PR verification failed.

Fix the deterministic local failure, commit the fix, and leave `.coding-complete` in place only when the controller should rerun verification.

Command: `{COMMAND}`
Exit code: `{EXIT_CODE}`

Bounded log excerpt:

```text
{LOG_EXCERPT}
```

[project]
name = "wavemill-dspy"
version = "0.1.0"
description = "DSPy-optimized agent selector for wavemill model router"
requires-python = ">=3.10"
dependencies = [
    "dspy>=2.5",
    "anthropic>=0.39",
]

[project.optional-dependencies]
dev = ["pytest"]

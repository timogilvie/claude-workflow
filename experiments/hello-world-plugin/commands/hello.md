---
description: Return a short hello response for plugin smoke testing.
argument-hint: [name]
---

Return exactly one line: `Hello from wavemill-hello-world`.

If `$ARGUMENTS` is non-empty, append `, $ARGUMENTS` to that same line.
